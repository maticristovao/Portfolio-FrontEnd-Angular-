import { ElementObserverDirective } from './element-observer.directive';

describe('ElementObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementObserverDirective();
    expect(directive).toBeTruthy();
  });
});
